import React, { useReducer } from "react"
import Todo from "./Todo"


function TodoList (props) {

    return (
        <div>
            {/* {props.data.map(cv => <Todo key = {cv.id} cv = {cv} />)} */}
            {/* {state.map(cv => <div><li key = {cv.id}>{cv.item}</li></div>)} */}
        </div>
    )
}

export default TodoList